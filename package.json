{
  "name": "meaningfool-report-agents-for-the-rest-of-us",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.js",
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^17.0.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^7.3.1"
  }
}
